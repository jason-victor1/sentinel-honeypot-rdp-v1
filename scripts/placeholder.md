# Scripts

V1 does not require scripts.

This folder is reserved for future versions, such as:

- V2 analytics rule deployment helpers
- V3 incident response automation helpers
- V4 custom telemetry pipeline scripts
